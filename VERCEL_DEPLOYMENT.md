# Vercel deployment

See **[../docs/deployment/VERCEL_DEPLOYMENT.md](../docs/deployment/VERCEL_DEPLOYMENT.md)** for the full guide. Summary: set `NEXT_PUBLIC_API_BASE_URL` to your backend URL; use root directory `frontend` if deploying from the monorepo.
